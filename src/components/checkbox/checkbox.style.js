import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
    borderRadius: 5,
    borderWidth: 1,
  },
  checkbox: {
    width: 20,
    height: 20,
    marginRight: 10,
    borderRadius: 3,
  },
  label: {
    fontSize: 16,
  },
});
